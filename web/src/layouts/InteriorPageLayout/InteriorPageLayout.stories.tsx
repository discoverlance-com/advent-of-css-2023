import type { Meta, StoryObj } from '@storybook/react'

import InteriorPageLayout from './InteriorPageLayout'

const meta: Meta<typeof InteriorPageLayout> = {
  component: InteriorPageLayout,
}

export default meta

type Story = StoryObj<typeof InteriorPageLayout>

export const Primary: Story = {}
